import LoginMain from '@domain/auth/components/LoginMain';
import { FC, ReactElement } from 'react';

const Login: FC = function loginPage(): ReactElement {
  return <LoginMain />;
};

export default Login;
