import AuthPagesRoutes from '@domain/auth/routes/AuthPagesRoutes';
import { FC, ReactElement } from 'react';

const Auth: FC = (): ReactElement => {
  return <AuthPagesRoutes />;
};

export default Auth;
